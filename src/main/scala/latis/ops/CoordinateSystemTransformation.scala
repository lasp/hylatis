package latis.ops

case class CoordinateSystemTransformation() { //extends UnaryOperation {
  /*
   * Allow this to be composed with other operations
   * avoid shuffling afger temporary change of domain values
   * MappingOperation: Sample => Sample
   * in terms of DomainData => DomainData
   * as a function that can be used in composition
   * 
   * can we compose at the sample level?
   * 
   * 
   */
  
  /*
   * TODO: when should we deal with ordering?
   */
}
